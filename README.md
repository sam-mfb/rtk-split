# rtk-split

Proof of concept of package splitting an rtk app

To run, from the repo root:

```
npm install @microsoft/rush
rush install
rush update
cd counter
rushx build
cd ../main-app
rushx start
```
